;
; upstream config {
;     server api:8000;
; }
;
; server {
;
;     listen 80;
;
;     location / {
;         proxy_pass http://api:8000;
;         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
;         proxy_set_header Host $host;
;         proxy_redirect off;
;         client_max_body_size 100M;
;     }
;
;     location /static/ {
;         root /usr/src/app;
;     }
;
;     location /media/ {
;         root /usr/src/app;
;     }
; }
upstream config {
    server api:8000;
}

server {
    listen 80;
    server_name alkhail.pp.ua;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name alkhail.pp.ua;

    ssl_certificate /etc/nginx/ssl/alkhail.pp.ua/certificate.crt;
    ssl_certificate_key /etc/nginx/ssl/alkhail.pp.ua/private.key;

    # Дополнительные настройки SSL...
    ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
    ssl_prefer_server_ciphers on;
    ssl_ciphers 'EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH';

    location / {
        proxy_pass http://config;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
        client_max_body_size 100M;
    }

    location /static/ {
        alias /usr/src/app/static/;
    }

    location /media/ {
        alias /usr/src/app/media/;
    }
}
